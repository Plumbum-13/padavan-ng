--- ZeroTierOne-1.16.0.orig/ext/miniupnpc/listdevices.c
+++ ZeroTierOne-1.16.0/ext/miniupnpc/listdevices.c
@@ -77,6 +77,10 @@
 		}
 	}
 
+#ifndef CONFIG_IPV6
+	ipv6 = 0;
+#endif
+
 	if(searched_device) {
 		printf("searching UPnP device type %s\n", searched_device);
 		devlist = upnpDiscoverDevice(searched_device,
--- ZeroTierOne-1.16.0.orig/ext/miniupnpc/minissdpc.c
+++ ZeroTierOne-1.16.0/ext/miniupnpc/minissdpc.c
@@ -526,11 +526,13 @@
 	/* reception */
 	memset(&sockudp_r, 0, sizeof(struct sockaddr_storage));
 	if(ipv6) {
+#ifdef CONFIG_IPV6
 		struct sockaddr_in6 * p = (struct sockaddr_in6 *)&sockudp_r;
 		p->sin6_family = AF_INET6;
 		if(localport > 0 && localport < 65536)
 			p->sin6_port = htons((unsigned short)localport);
 		p->sin6_addr = in6addr_any; /* in6addr_any is not available with MinGW32 3.4.2 */
+#endif
 	} else {
 		struct sockaddr_in * p = (struct sockaddr_in *)&sockudp_r;
 		p->sin_family = AF_INET;
--- ZeroTierOne-1.16.0.orig/ext/miniupnpc/upnpc.c
+++ ZeroTierOne-1.16.0/ext/miniupnpc/upnpc.c
@@ -666,6 +666,10 @@
 		return 1;
 	}
 
+#ifndef CONFIG_IPV6
+	ipv6 = 0;
+#endif
+
 	if( rootdescurl
 	  || (devlist = upnpDiscover(2000, multicastif, minissdpdpath,
 	                             localport, ipv6, ttl, &error)))
--- ZeroTierOne-1.16.0.orig/service/OneService.cpp
+++ ZeroTierOne-1.16.0/service/OneService.cpp
@@ -2639,6 +2639,7 @@
 		}
 
 		bool v6controlPlaneBound = false;
+#ifdef CONFIG_IPV6
 		_controlPlaneV6.set_address_family(AF_INET6);
 		if (_controlPlaneV6.bind_to_port("::", _primaryPort)) {
 			_serverThreadV6 = std::thread([&] {
@@ -2656,7 +2657,7 @@
 			fprintf(stderr, "Error binding control plane to [::]:%d\n", _primaryPort);
 			v6controlPlaneBound = false;
 		}
-
+#endif
 		if (! v4controlPlaneBound && ! v6controlPlaneBound) {
 			fprintf(stderr, "ERROR: Could not bind control plane. Exiting...\n");
 			exit(-1);
